# niflix_project/.htaccess

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /niflix_project/  # PASTIKAN INI SESUAI DENGAN SUBFOLDER PROYEKMU

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [L]
</IfModule>
